/**
 * 动态加载小程序组件编辑器
 */
export function loadMpEditorFromBundle (bundleDefinition) {
    let {
        groupId, artifactId, version, classifier
    } = bundleDefinition;
    // 请求代理 获取js css

    // 使用requireJS加载

}

export function loadMpComponentFromBundle (bundleDefinition) {
    let {
        groupId, artifactId, version, classifier
    } = bundleDefinition;

}
