@import './_variable.pcss';
@import './theme.pcss';

.mp-design-preview {
  border: 1px solid $theme-stroke-7;
  width: $preview-width;
  min-height: $preview-height;
  position: relative;

  .mp-design-add-component-overlay--grouped {
    background: $theme-stroke-10;

    &:after {
      border-right-color: $theme-stroke-10;
    }
  }
}
